import * as React from "react"
import { Link } from "gatsby"

const Header = ({ siteTitle }) => (
  <header
    style={{
      backgroundColor: "red",
    }}
  >
    <h1>Header</h1>
  </header>
)

export default Header
